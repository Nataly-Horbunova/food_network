const HeaderNavItem = ({text}) => {
    return (
        <li className="header-nav-item">
            <a href="#" className="header-nav-link">{text}</a>
        </li>
    )
}

export default HeaderNavItem;